import type { MarketProvider } from "./types";

// Future provider placeholder. Keep real provider credentials server-side only.
// TODO: Implement Tradier quote, expiration, and options-chain requests.
export const tradierProvider: MarketProvider | null = null;
